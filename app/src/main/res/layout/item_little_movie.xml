<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    android:background="@color/colorBaseText"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground"
    android:theme="@style/AppTheme">

    <ImageView
        android:id="@+id/posterItemMovieHome"
        android:layout_width="wrap_content"
        android:layout_height="166dp"
        android:adjustViewBounds="true"
        android:contentDescription="@string/posterMovieContentDesc"
        android:padding="2dp"
        android:scaleType="fitXY"
        android:transitionName="@string/transitionMovieListToDetail"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/example_poster" />

    <ImageButton
        android:id="@+id/addToFavoriteLittleMovie"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@android:color/transparent"
        android:padding="5dp"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="@+id/posterItemMovieHome"
        app:layout_constraintTop_toTopOf="@+id/posterItemMovieHome"
        app:srcCompat="@drawable/ic_favorite_border_red_24dp" />
</androidx.constraintlayout.widget.ConstraintLayout>